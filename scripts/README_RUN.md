# Running the demo components

Use the run_demo.sh script for guidance. Components:
- MQTT Broker (mosquitto)
- Backend Flask server
- Edge simulator (vehicle_counter.py)
- Dashboard (static HTML, file open or serve with `python -m http.server 8000` inside dashboard/)
